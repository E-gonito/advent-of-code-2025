f.split(",")
invalidId = 0
for each line in f:
    line.split("-")
    lower, upper
    for x in range (lower, upper):
        if magnitude of x % 2 == 0 (the number of digits in the number is even e.g 1000, 10, 100,000):
            check if subnumber is repeated:
                invalidID += ID
return invalidID
